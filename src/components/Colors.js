import config from "../pages/components/charts/config";

export const colors = config.chartColors;
export const barColors = [
  colors.blue,
  colors.green,
  colors.red,
  colors.orange,
  colors.pink,
  colors.purple,
  colors.teal
];